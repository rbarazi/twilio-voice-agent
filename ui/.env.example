# Optional: Override API URL for SSR contexts
# If not set, the UI will automatically detect the domain from window.location
# NEXT_PUBLIC_API_URL=https://your-tunnel-domain.com

# Optional: Override WebSocket URL for SSR contexts
# If not set, the UI will automatically use wss:// for https and ws:// for http
# NEXT_PUBLIC_WS_URL=wss://your-tunnel-domain.com
