import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./ui/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
